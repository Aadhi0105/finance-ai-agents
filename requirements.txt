# Offline skeleton (python run.py) needs NOTHING beyond the stdlib.
# These are only for the live path (python run.py --live TICKER):
anthropic>=0.40.0
yfinance>=0.2.40
python-dotenv>=1.0.0
matplotlib>=3.7

# Arriving at later checkpoints (charts, factor model):
# pandas>=2.0
# numpy>=1.26
# matplotlib>=3.8
# statsmodels>=0.14
